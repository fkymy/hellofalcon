memo

 - just a wsgi callable

 - pypy easiest setup

 - venv
source .venv/bin/activate

 - gunicorn with code reload
gunicorn --reload look.app

 - ipython (just use standard with help())

 - httpie (http localhost:8000/path/to/resource)

 - 

           ┌────────────┐
request  → │            │
           │ Resource   │ ↻ Orchestrate the requested action
           │ Controller │ ↻ Compose the result
response ← │            │
           └────────────┘

 - msgpack-python

 - pytest
